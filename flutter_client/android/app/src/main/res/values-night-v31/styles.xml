<?xml version="1.0" encoding="utf-8"?>
<!--
  The dark-mode counterpart of values-v31/styles.xml. Needed because the night
  qualifier outranks the version one: without this file, values-night/styles.xml
  would win on Android 12+ in dark mode and lose the system-splash attributes.
-->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
