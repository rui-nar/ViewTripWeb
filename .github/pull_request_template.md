## Summary

<!-- What changed and why. -->

## Test plan

- [ ] `pytest tests/` passes (and `alembic check` if models/migrations changed)
- [ ] `flutter analyze` / `flutter test` passes (if `flutter_client/` changed)

## Release notes

- [ ] No user-visible change — subject line is enough
- [ ] Added a `Release-Note:` trailer on the relevant commit(s)
- [ ] Added an `Upgrade-Note:` trailer (breaking/config change)
