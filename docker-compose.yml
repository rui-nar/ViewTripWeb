services:
  viewtripweb:
    image: ghcr.io/rui-nar/viewtripweb:latest
    ports:
      - "3000:3000"   # Reflex frontend
      - "8000:8000"   # Reflex backend API
    volumes:
      - /volume1/docker/viewtripweb/data:/data       # projects, tokens, cache
      - /volume1/docker/viewtripweb/config:/app/config  # config.json with Strava credentials
    environment:
      - GETTRACKS_DATA_DIR=/data
    restart: unless-stopped
